# Sourcing the functions file
source("functions.R")

# Loading the required libraries
library(ggplot2)
library(dplyr)

baseline <- simulate.OLS(muX=10, vX=4, muE=2, vE=1, r=0, n=1000,
                         N=1000, b0=9, b1=2)

y <- rnorm(n=100, mean=2, sd=1)
x <- rnorm(n=100, mean=5, sd=4)

# Defining \tilde{F}

plot(df(x=seq(0, 5, length=100), df1=10, df=1), type="l", lwd=2)










